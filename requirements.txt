# Ranged pins so scripts run on Python 3.8+ (Pillow 11 dropped 3.8;
# Pillow 10 is the last line that keeps 3.8 support). CI + the
# nightly workflow use 3.12 so they'll resolve the latest anyway.
httpx>=0.27,<1
Pillow>=10,<12
python-dotenv>=1,<2
